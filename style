/* ==========================================================================
   Meu Painel de Metas — Design System
   Paleta escura "financeira premium" com suporte a tema claro.
   ========================================================================== */

:root {
  --accent: #6C5CE7;        /* violeta — marca / ações primárias */
  --accent-2: #14B8A6;      /* teal — progresso / sucesso */
  --warning: #F5A524;       /* âmbar — alertas / prazos */
  --danger: #EF4444;        /* vermelho — exclusão / prioridade alta */

  --bg-app: #0B0E13;
  --bg-card: #141922;
  --bg-card-2: #1B212C;
  --bg-elevated: #202836;
  --border: rgba(255, 255, 255, 0.08);
  --border-strong: rgba(255, 255, 255, 0.14);

  --text-primary: #F3F5F8;
  --text-secondary: #9AA4B2;
  --text-tertiary: #6B7686;

  --radius-sm: 10px;
  --radius-md: 16px;
  --radius-lg: 22px;
  --shadow-card: 0 8px 24px rgba(0, 0, 0, 0.35);
  --shadow-pop: 0 16px 48px rgba(0, 0, 0, 0.5);

  --font-display: "Sora", "Inter", sans-serif;
  --font-body: "Inter", sans-serif;

  --safe-bottom: env(safe-area-inset-bottom, 0px);
  --safe-top: env(safe-area-inset-top, 0px);
}

[data-theme="light"] {
  --bg-app: #F4F5F7;
  --bg-card: #FFFFFF;
  --bg-card-2: #FBFBFD;
  --bg-elevated: #FFFFFF;
  --border: rgba(15, 23, 42, 0.08);
  --border-strong: rgba(15, 23, 42, 0.14);

  --text-primary: #12151C;
  --text-secondary: #5B6472;
  --text-tertiary: #8A93A1;

  --shadow-card: 0 6px 20px rgba(15, 23, 42, 0.08);
  --shadow-pop: 0 20px 48px rgba(15, 23, 42, 0.16);
}

* {
  box-sizing: border-box;
}

html, body {
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

body {
  background: var(--bg-app);
  color: var(--text-primary);
  font-family: var(--font-body);
  -webkit-font-smoothing: antialiased;
  min-height: 100vh;
  transition: background 0.25s ease, color 0.25s ease;
}

h1, h2, h3, h4 {
  font-family: var(--font-display);
  margin: 0;
  font-weight: 700;
}

p {
  margin: 0;
}

button {
  font-family: var(--font-body);
  cursor: pointer;
}

input, select, textarea {
  font-family: var(--font-body);
}

a {
  color: inherit;
}

::selection {
  background: var(--accent);
  color: white;
}

:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

.visually-hidden {
  position: absolute;
  width: 1px; height: 1px;
  overflow: hidden;
  clip: rect(0 0 0 0);
  white-space: nowrap;
}

/* ---------- Layout raiz ---------- */

#app {
  max-width: 1100px;
  margin: 0 auto;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.app-body {
  flex: 1;
  display: flex;
}

.screen {
  display: none;
  flex: 1;
  padding: 20px 16px calc(96px + var(--safe-bottom));
  animation: fadeIn 0.25s ease;
}

.screen.active {
  display: block;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(6px); }
  to { opacity: 1; transform: translateY(0); }
}

@media (min-width: 900px) {
  .screen {
    padding: 32px 40px 40px;
  }
}

/* ---------- Topbar ---------- */

.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: calc(16px + var(--safe-top)) 16px 8px;
}

.topbar h1 {
  font-size: 20px;
}

.brand {
  display: flex;
  align-items: center;
  gap: 10px;
}

.brand-mark {
  width: 34px; height: 34px;
  border-radius: 10px;
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  display: flex; align-items: center; justify-content: center;
  font-size: 16px;
}

.icon-btn {
  width: 40px; height: 40px;
  border-radius: 12px;
  border: 1px solid var(--border);
  background: var(--bg-card);
  color: var(--text-primary);
  display: flex; align-items: center; justify-content: center;
  font-size: 18px;
  transition: transform 0.15s ease, background 0.15s ease;
}

.icon-btn:active {
  transform: scale(0.94);
}

/* ---------- Navegação inferior (mobile) ---------- */

.bottom-nav {
  position: fixed;
  left: 0; right: 0; bottom: 0;
  display: flex;
  justify-content: space-around;
  background: var(--bg-card);
  border-top: 1px solid var(--border);
  padding: 8px 4px calc(8px + var(--safe-bottom));
  z-index: 50;
  max-width: 1100px;
  margin: 0 auto;
}

.nav-item {
  background: none;
  border: none;
  color: var(--text-tertiary);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  font-size: 10px;
  padding: 6px 8px;
  border-radius: 10px;
  flex: 1;
  min-width: 0;
  transition: color 0.15s ease;
}

.nav-item .nav-icon {
  font-size: 19px;
  line-height: 1;
}

.nav-item.active {
  color: var(--accent);
}

/* ---------- Desktop: navegação lateral ---------- */

@media (min-width: 900px) {
  .bottom-nav {
    position: static;
    flex-direction: column;
    justify-content: flex-start;
    align-items: stretch;
    width: 220px;
    border-top: none;
    border-right: 1px solid var(--border);
    padding: 24px 12px;
    gap: 4px;
    max-width: none;
  }

  .nav-item {
    flex-direction: row;
    justify-content: flex-start;
    gap: 10px;
    font-size: 14px;
    padding: 10px 12px;
  }

  .nav-item .nav-icon {
    font-size: 18px;
  }

  .app-body {
    flex-direction: row;
  }

  .screen {
    flex: 1;
  }
}

/* ---------- Cartões ---------- */

.card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
  padding: 18px;
}

.section-title {
  font-size: 13px;
  color: var(--text-tertiary);
  text-transform: none;
  letter-spacing: 0.2px;
  margin: 24px 0 10px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.section-title .link-btn {
  font-size: 13px;
  color: var(--accent);
  background: none;
  border: none;
  font-weight: 600;
}

/* ---------- Painel: saudação e resumo ---------- */

.greeting h2 {
  font-size: 24px;
  margin-bottom: 4px;
}

.greeting p {
  color: var(--text-secondary);
  font-size: 14px;
}

.summary-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
  margin-top: 16px;
}

.summary-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  padding: 16px;
  box-shadow: var(--shadow-card);
}

.summary-card .label {
  font-size: 12px;
  color: var(--text-tertiary);
  margin-bottom: 6px;
}

.summary-card .value {
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 700;
}

.summary-card .value.accent { color: var(--accent-2); }
.summary-card .value.warn { color: var(--warning); }

/* ---------- Foco atual (meta principal) ---------- */

.focus-card {
  margin-top: 18px;
  background: linear-gradient(160deg, var(--bg-card-2), var(--bg-card));
  border: 1px solid var(--border-strong);
  border-radius: var(--radius-lg);
  padding: 20px;
  box-shadow: var(--shadow-pop);
  position: relative;
  overflow: hidden;
}

.focus-eyebrow {
  font-size: 12px;
  color: var(--accent-2);
  font-weight: 600;
  margin-bottom: 10px;
  display: flex;
  align-items: center;
  gap: 6px;
}

.focus-name {
  font-size: 19px;
  margin-bottom: 4px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.focus-amounts {
  font-family: var(--font-display);
  font-size: 15px;
  color: var(--text-secondary);
  margin-bottom: 14px;
}

.focus-amounts strong {
  color: var(--text-primary);
  font-size: 22px;
}

.focus-meta-row {
  display: flex;
  justify-content: space-between;
  margin-top: 12px;
  font-size: 13px;
  color: var(--text-secondary);
  flex-wrap: wrap;
  gap: 8px;
}

.focus-savings {
  margin-top: 16px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
}

.savings-chip {
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 10px;
  text-align: center;
}

.savings-chip .amount {
  font-family: var(--font-display);
  font-size: 15px;
  font-weight: 700;
  color: var(--accent-2);
}

.savings-chip .label {
  font-size: 10px;
  color: var(--text-tertiary);
  margin-top: 2px;
}

/* ---------- Barra de progresso ---------- */

.progress-track {
  width: 100%;
  height: 12px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.06);
  overflow: hidden;
  position: relative;
}

[data-theme="light"] .progress-track {
  background: rgba(15, 23, 42, 0.07);
}

.progress-fill {
  height: 100%;
  border-radius: 999px;
  background: linear-gradient(90deg, var(--accent), var(--accent-2));
  transition: width 0.6s cubic-bezier(0.22, 1, 0.36, 1);
}

.progress-fill.complete {
  background: linear-gradient(90deg, var(--accent-2), #34D399);
}

.progress-label-row {
  display: flex;
  justify-content: space-between;
  font-size: 12px;
  color: var(--text-tertiary);
  margin-top: 6px;
}

/* ---------- Insights do dashboard ---------- */

.insights-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-top: 16px;
}

.insight-item {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 12px 14px;
  font-size: 13.5px;
  display: flex;
  align-items: center;
  gap: 10px;
}

/* ---------- Lista de metas ---------- */

.toolbar-row {
  display: flex;
  gap: 8px;
  margin-bottom: 14px;
  flex-wrap: wrap;
}

.search-input {
  flex: 1;
  min-width: 140px;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 10px 14px;
  color: var(--text-primary);
  font-size: 14px;
}

.search-input::placeholder {
  color: var(--text-tertiary);
}

.select-input {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 10px 12px;
  color: var(--text-primary);
  font-size: 13px;
}

.goals-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 14px;
}

@media (min-width: 640px) {
  .goals-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1000px) {
  .goals-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

.goal-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  padding: 16px;
  box-shadow: var(--shadow-card);
  display: flex;
  flex-direction: column;
  gap: 10px;
  transition: transform 0.15s ease;
}

.goal-card.completed {
  border-color: rgba(20, 184, 166, 0.4);
}

.goal-card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

.goal-title {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 15px;
  font-weight: 600;
  min-width: 0;
}

.goal-title .icon {
  font-size: 20px;
}

.goal-title .name {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.priority-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}

.priority-dot.alta { background: var(--danger); }
.priority-dot.media { background: var(--warning); }
.priority-dot.baixa { background: var(--accent-2); }

.goal-amounts {
  font-family: var(--font-display);
}

.goal-amounts .current {
  font-size: 22px;
  font-weight: 700;
}

.goal-amounts .target {
  font-size: 13px;
  color: var(--text-tertiary);
  font-family: var(--font-body);
}

.goal-meta-line {
  display: flex;
  justify-content: space-between;
  font-size: 12.5px;
  color: var(--text-secondary);
  flex-wrap: wrap;
  gap: 6px;
}

.goal-actions {
  display: flex;
  gap: 8px;
  margin-top: 4px;
}

.badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-size: 11px;
  padding: 3px 9px;
  border-radius: 999px;
  background: rgba(108, 92, 231, 0.14);
  color: var(--accent);
  font-weight: 600;
}

.badge.success {
  background: rgba(20, 184, 166, 0.16);
  color: var(--accent-2);
}

/* ---------- Botões ---------- */

.btn {
  border: none;
  border-radius: var(--radius-sm);
  padding: 11px 16px;
  font-size: 14px;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  transition: transform 0.12s ease, opacity 0.12s ease;
  min-height: 44px;
}

.btn:active {
  transform: scale(0.97);
}

.btn-primary {
  background: linear-gradient(135deg, var(--accent), #8B7CF6);
  color: white;
}

.btn-secondary {
  background: var(--bg-elevated);
  color: var(--text-primary);
  border: 1px solid var(--border-strong);
}

.btn-ghost {
  background: transparent;
  color: var(--text-secondary);
}

.btn-danger {
  background: rgba(239, 68, 68, 0.12);
  color: var(--danger);
}

.btn-block {
  width: 100%;
}

.btn-sm {
  padding: 8px 12px;
  font-size: 13px;
  min-height: 36px;
}

.fab-new-goal {
  position: fixed;
  right: 16px;
  bottom: calc(84px + var(--safe-bottom));
  z-index: 40;
  border-radius: 999px;
  padding: 14px 20px;
  box-shadow: var(--shadow-pop);
}

@media (min-width: 900px) {
  .fab-new-goal {
    right: 40px;
    bottom: 40px;
  }
}

/* ---------- Estado vazio ---------- */

.empty-state {
  text-align: center;
  padding: 48px 20px;
  color: var(--text-secondary);
}

.empty-state .emoji {
  font-size: 40px;
  margin-bottom: 12px;
}

.empty-state h3 {
  font-size: 17px;
  margin-bottom: 6px;
  color: var(--text-primary);
}

.empty-state p {
  font-size: 13.5px;
  max-width: 320px;
  margin: 0 auto 16px;
}

/* ---------- Modais / Bottom sheets ---------- */

.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.55);
  display: none;
  align-items: flex-end;
  justify-content: center;
  z-index: 100;
  backdrop-filter: blur(2px);
}

.modal-overlay.active {
  display: flex;
}

@media (min-width: 640px) {
  .modal-overlay {
    align-items: center;
  }
}

.modal-sheet {
  background: var(--bg-card);
  border-radius: 22px 22px 0 0;
  width: 100%;
  max-width: 520px;
  max-height: 88vh;
  overflow-y: auto;
  padding: 20px 20px calc(24px + var(--safe-bottom));
  animation: slideUp 0.25s cubic-bezier(0.22, 1, 0.36, 1);
  border: 1px solid var(--border-strong);
  border-bottom: none;
}

@media (min-width: 640px) {
  .modal-sheet {
    border-radius: 22px;
    border-bottom: 1px solid var(--border-strong);
  }
}

@keyframes slideUp {
  from { transform: translateY(24px); opacity: 0.6; }
  to { transform: translateY(0); opacity: 1; }
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
}

.modal-header h3 {
  font-size: 17px;
}

.modal-sheet .close-btn {
  background: none;
  border: none;
  color: var(--text-tertiary);
  font-size: 22px;
  line-height: 1;
  padding: 4px;
}

/* ---------- Formulários ---------- */

.form-group {
  margin-bottom: 14px;
}

.form-group label {
  display: block;
  font-size: 12.5px;
  color: var(--text-secondary);
  margin-bottom: 6px;
  font-weight: 500;
}

.form-control {
  width: 100%;
  background: var(--bg-elevated);
  border: 1px solid var(--border-strong);
  border-radius: var(--radius-sm);
  padding: 12px 14px;
  color: var(--text-primary);
  font-size: 15px;
}

.form-control:focus {
  border-color: var(--accent);
}

textarea.form-control {
  resize: vertical;
  min-height: 70px;
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}

.chip-group {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.chip-option {
  border: 1px solid var(--border-strong);
  background: var(--bg-elevated);
  color: var(--text-secondary);
  border-radius: 999px;
  padding: 8px 14px;
  font-size: 13px;
  display: flex;
  align-items: center;
  gap: 6px;
}

.chip-option.selected {
  border-color: var(--accent);
  background: rgba(108, 92, 231, 0.14);
  color: var(--text-primary);
}

.error-text {
  color: var(--danger);
  font-size: 12px;
  margin-top: 4px;
  display: none;
}

.error-text.visible {
  display: block;
}

.form-control.invalid {
  border-color: var(--danger);
}

/* ---------- Detalhes da meta ---------- */

.detail-hero {
  text-align: center;
  padding: 8px 0 18px;
}

.detail-hero .icon {
  font-size: 42px;
  display: block;
  margin-bottom: 8px;
}

.detail-hero .amounts {
  font-family: var(--font-display);
  font-size: 30px;
  font-weight: 700;
  margin-top: 10px;
}

.detail-hero .amounts small {
  font-size: 16px;
  color: var(--text-tertiary);
  font-weight: 500;
}

.stat-row {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 10px;
  margin-top: 18px;
}

.stat-box {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 12px;
  text-align: center;
}

.stat-box .value {
  font-family: var(--font-display);
  font-size: 17px;
  font-weight: 700;
}

.stat-box .label {
  font-size: 11px;
  color: var(--text-tertiary);
  margin-top: 3px;
}

.history-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 0;
  border-bottom: 1px solid var(--border);
  gap: 10px;
}

.history-item:last-child {
  border-bottom: none;
}

.history-item .date-group {
  font-size: 11px;
  color: var(--text-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.4px;
  margin: 18px 0 6px;
}

.history-amount {
  font-family: var(--font-display);
  font-weight: 700;
  color: var(--accent-2);
}

.history-note {
  font-size: 12.5px;
  color: var(--text-tertiary);
}

.history-meta {
  font-size: 12px;
  color: var(--text-tertiary);
}

/* ---------- Conquistas ---------- */

.achievements-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
}

@media (min-width: 640px) {
  .achievements-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

.achievement-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  padding: 14px;
  text-align: center;
  opacity: 0.42;
  filter: grayscale(0.6);
}

.achievement-card.unlocked {
  opacity: 1;
  filter: none;
  border-color: rgba(108, 92, 231, 0.35);
  box-shadow: var(--shadow-card);
}

.achievement-card .icon {
  font-size: 28px;
  display: block;
  margin-bottom: 8px;
}

.achievement-card .title {
  font-size: 12.5px;
  font-weight: 600;
  margin-bottom: 4px;
}

.achievement-card .desc {
  font-size: 10.5px;
  color: var(--text-tertiary);
}

/* ---------- Análises ---------- */

.chart-card {
  margin-bottom: 16px;
}

.chart-card h3 {
  font-size: 14px;
  margin-bottom: 12px;
}

.chart-card canvas {
  width: 100%;
  display: block;
}

.donut-legend {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 12px;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: var(--text-secondary);
}

.legend-dot {
  width: 9px; height: 9px;
  border-radius: 50%;
}

/* ---------- Configurações ---------- */

.settings-group {
  margin-bottom: 22px;
}

.settings-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 0;
  border-bottom: 1px solid var(--border);
  gap: 12px;
}

.settings-row:last-child {
  border-bottom: none;
}

.settings-row .row-label {
  font-size: 14px;
  font-weight: 500;
}

.settings-row .row-desc {
  font-size: 12px;
  color: var(--text-tertiary);
  margin-top: 2px;
}

.theme-options {
  display: flex;
  gap: 6px;
  background: var(--bg-elevated);
  border-radius: 999px;
  padding: 4px;
}

.theme-options button {
  border: none;
  background: transparent;
  color: var(--text-secondary);
  padding: 7px 12px;
  border-radius: 999px;
  font-size: 12.5px;
}

.theme-options button.active {
  background: var(--accent);
  color: white;
}

/* ---------- Toasts ---------- */

.toast-container {
  position: fixed;
  top: calc(16px + var(--safe-top));
  left: 50%;
  transform: translateX(-50%);
  z-index: 200;
  display: flex;
  flex-direction: column;
  gap: 8px;
  width: min(90vw, 380px);
}

.toast {
  background: var(--bg-elevated);
  border: 1px solid var(--border-strong);
  border-radius: var(--radius-sm);
  padding: 12px 16px;
  font-size: 13.5px;
  box-shadow: var(--shadow-pop);
  animation: toastIn 0.2s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.toast.success { border-color: rgba(20, 184, 166, 0.5); }
.toast.error { border-color: rgba(239, 68, 68, 0.5); }

@keyframes toastIn {
  from { opacity: 0; transform: translateY(-8px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ---------- Confetes (celebração de meta concluída) ---------- */

.confetti-layer {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 300;
  overflow: hidden;
}

.confetti-piece {
  position: absolute;
  top: -12px;
  width: 8px;
  height: 14px;
  opacity: 0.9;
  animation: confettiFall linear forwards;
}

@keyframes confettiFall {
  to {
    transform: translateY(105vh) rotate(540deg);
    opacity: 0.2;
  }
}

/* ---------- Onboarding ---------- */

.onboarding-screen {
  position: fixed;
  inset: 0;
  background: radial-gradient(circle at 30% 20%, rgba(108, 92, 231, 0.25), transparent 55%),
              radial-gradient(circle at 80% 80%, rgba(20, 184, 166, 0.2), transparent 55%),
              var(--bg-app);
  z-index: 500;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 32px;
}

.onboarding-screen .mark {
  width: 72px; height: 72px;
  border-radius: 20px;
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  display: flex; align-items: center; justify-content: center;
  font-size: 34px;
  margin-bottom: 24px;
  box-shadow: var(--shadow-pop);
}

.onboarding-screen h1 {
  font-size: 26px;
  max-width: 320px;
  margin-bottom: 10px;
}

.onboarding-screen p {
  color: var(--text-secondary);
  max-width: 300px;
  margin-bottom: 32px;
  font-size: 14.5px;
}

/* ---------- Confirmação (dialog genérico) ---------- */

.confirm-actions {
  display: flex;
  gap: 10px;
  margin-top: 18px;
}

.confirm-actions .btn {
  flex: 1;
}

/* ---------- Utilitários ---------- */

.flex-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.mt-8 { margin-top: 8px; }
.mt-16 { margin-top: 16px; }
.text-secondary { color: var(--text-secondary); }
.text-tertiary { color: var(--text-tertiary); }
.hidden { display: none !important; }

@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.001ms !important;
    transition-duration: 0.001ms !important;
  }
}
